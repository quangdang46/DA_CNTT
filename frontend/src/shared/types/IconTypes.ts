import { SizeProp } from "@fortawesome/fontawesome-svg-core"; // Import SizeProp
interface IconType {
  color?: string; // Màu sắc (tuỳ chọn)
  size?: SizeProp; // Kích thước (tuỳ chọn)
}

export default IconType;